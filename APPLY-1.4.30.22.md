# RabbitMirror 1.4.30.22

基线：1.4.30.21（包含真实渲染反馈 overlay 与配色冷却）。

本版只处理三类问题：

1. 主 API 在输出协议前、独立 API 在近输出短锁中携带具体展现形式契约，并加入有效视觉程序与 360px 数量语义群组验收。
2. 纯文字救援不再因“存在任意 CSS 规则”或“超过一个控件”直接退出；允许最多八个 radio/checkbox 的叙事选择，同时继续拒绝文本输入、按钮、select、textarea 等真实表单。降级 CSS 取消通用圆角卡片外壳。
3. 横向裁切救援先识别 3–8 个简短标注人物／关系节点组成的语义群组；轻度超宽时整体缩放进入首屏，超过安全缩放阈值才保留原横向滚动兜底。
4. 入口、UI、生成器、清洗器与视觉扫描器的模块查询版本统一为 1.4.30.22，避免浏览器同时复用新旧模块实例。

安全边界不变：不新增脚本执行、事件属性、全局 CSS、外部网络或持久化 DOM 标记；纯文字与语义群组巡检均在布局读取前设置 DOM 工作预算，超限保守跳过救援。
