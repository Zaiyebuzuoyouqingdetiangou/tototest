# 1.5.35 候选验证记录

整合验证完成时间：2026-09-09 13:27:49 UTC。对象为同一份冻结的 1.5.35-stability1 源码，不引用旧版本的测试数量充当本次结果。

## 本地结果

- 113 个单元测试文件，共 494 项通过，0 失败、0 跳过。
- 212 个 JS/MJS 文件语法检查通过；库索引重建校验通过（展现 208、主题 165）。
- 15 组 Edge 浏览器夹具通过：首次交互、镜面边界、独立交互保存、局部面板可见性、输入栏留空、被动动画生命周期、质量管线、成品保留、触摸弹窗、临时事务配额提示、标题浮动、新手指引/外部高级设置、历史手势生命周期、托管消息生命周期、TT 设置布局。
- 运行代码及 data 文件在整合测试前后 SHA-256 一致。打包时再次核对这些冻结摘要，拒绝夹入未经验证的新运行文件。
- 25 个 data 文件与 1.5.34 对照，仅 rawSegmentLookup.js 的两条导入缓存版本改变；母本内容和其他 data 字节未改。
- 原 1.5.34 正式 ZIP SHA-256 保持 `C71B307A75CFF6CCA55C0FD07115B40A8DBA0609CD199477E0E9102C1008E81A`。

## 可复现证据与限定

普通酒馆手势夹具：重复手势后新增安装 0、DOM 变动 0；原镜面、阅读容器、动画仍是同一对象，复选状态保留，局部滚动 170 和聊天滚动 40 均不变。真实宿主替换节点后仍能初始化新节点。旧版同夹具能证明重复安装，但没有复现用户视频的所有闪烁/回弹，因此不把一段视频归因为已经完全证实的单一原因。

TT 生命周期夹具：公开注册 1 次、fault 0；没有新增整段 #chat MutationObserver 或历史滚动恢复监听；根部非法节点 0，镜面及留空在消息所有者内部。内容版本更新后原镜面、留空、动画保留；整条消息卸载后清理绑定、临时计时器和弹窗监听，不擅自恢复虚化历史。TT-like 设置夹具覆盖 320/390/768px 及明暗两种环境，没有横向溢出，不改变宿主控件高度。

模板 UI 使用真实生产 ui.js 事件、settings 和浏览器 IndexedDB，验证保存不自动启用、原输入清空、原故事/外链不进摘要或 settings、开关正确、无服务器/模型调用，并检查了截图。跟随、独立和单面重说的预取测试覆盖关闭零读取、每批只读取一次及过期归属拒绝。异步保存中的关联版本变化不会被旧完成结果覆盖。

复查时发现并在发布前修复了传输解析回归：完整非流 JSON 不再误作嵌套 NDJSON，空 content 不取 reasoning，正文中的 [DONE] 不截断非流读取，非流失败不新增原文救援载荷，大小拦截字节不能伪称精确。这些用例先失败、修后通过。

配额夹具证明“最终占用能放下、写入中途却超额”的一类问题；改动仅调整既有缩小项与增长项的先后顺序。同一批次、相同保留策略、回滚原记录、不触碰历史/其他扩展、不追加 API 请求。真正空间不足仍会报错。

第一轮整合有 8 项旧测试环境/范围断言失败，已保留失败记录并修正：7 项补齐普通 ST 夹具的宿主适配接口，原行为断言不删除；1 项明确本次授权变更的 index、外部诊断、touchTheater，不伪造新基线哈希。其余 30 个旧基线保护文件继续按旧摘要核对。随后完整重跑通过。

## 不能据此声称

没有调用真实模型、真实供应商或用户云酒馆；没有 Android/iOS 实机、真实 TT 安装版本、温升/电池、跨设备一致性或模型生成速度验收。浏览器夹具不是完整运行中的 TT。不可保证任意美化、任意生成 HTML 或所有手机闪烁问题已解决。

Codex Process Jobs 在本机返回不支持 win32，后台任务未创建；本次采用当前会话中的有界前台验证，没有后台常驻进程或线上更改。
