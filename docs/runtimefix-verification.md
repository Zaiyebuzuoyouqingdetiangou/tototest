# 1.5.20 RuntimeFix1 验证与边界

日期：2026-09-06。基线：保留的 1.5.19 Usability1；在单独目录修订，原 ZIP、线上酒馆与 GitHub 未覆盖。

## 已有新鲜证据

- 全量 Node 回归 **364/364**；33 个受保护文件对照上传基线（仅移除发布标识及逐字列明的授权视觉改动）。192 个增强绘制关闭态样本保持其余旧规则。
- 9 项本地 Edge 浏览器脚本全部通过：导入/高级设置/冷恢复替换/运行时/部分留存/替换快照，以及本次长 ID、嵌套弹窗、触控性能脚本。
- 长中文文件名、45 条自创母本，经真实 File 导入、IndexedDB 保存和冷回载，再走生产批次计划：主/副两路 × 动态开/关 × 1～5 面共 20 组合通过；没有整库 raw `getAll`，没有 provider 请求；停用库仍拒绝。
- 发送前等待期间宿主最终回写，旧版会将失败放在过期 hash 下；新版将同一操作的错误卡、停止记录和诊断结算到当前精确身份。新对象同正文仍严格拒绝发送，错误可见且被动同步不重试；不同聊天、Swipe、epoch、显式替换不继承旧错误。
- 导入窗口在带 transform、滚动 1400px、原生父弹窗、320/390px 和横屏条件下通过 18 组检查，包括关闭/ESC/焦点恢复和读取中关闭。旧版关闭按钮 top=-589.75px 且命中失败；新版 top=32.25px 且命中成功。
- 五面原创场景、1045 节点、约 6.7 万字符，反复展开/折叠/切换/维修菜单 40 次：普通首次展开每面样式读取 689→18、几何读取 681→0；10 次交互加 10 次维修菜单的整条源码解码 35→1。同长度正文修改、显示源修改、Swipe 及其类型变化都会失效重算。
- 真正简单裁字区域 24px→284px；内部可控封面、line-clamp 和普通正常文本不误改。只排查局部高置信场景，不是解除全部 overflow。
- 真实装配 Prompt 证实动态多面混入通用页面交互规则，且完整动态规则允许“画面外承载后续正文”。已做分域和去冲突；未调温度、总输出/请求预算或每批请求次数，也未增加固定题材/配色模板。
- unclosed-raw-text 继续按结构错误拒绝，没有自动补结束标签、自动补发或假保存；仅明确的长度终止证据才提示上限问题。部分可信合格面留存行为保持。

## 已连接真实酒馆，但不是新包实机验收

用户连接的电脑 Edge 实际仍加载 `1.5.19-usability1`，没有在线部署本修订。只操作现有 UI，没有点击模型生成、重说、模型维修或读取/导出母本条目。

用户随后自行生成的五面成品：

- 页面处于独立 API、五面、增强绘制开启、强制动态视觉关闭；这是检查当时状态，不能推翻用户此前动态开启时的截图。
- 五个独立 details 均存在，标题约 22.05px；生产 DOM 确实命中新版的 inline 标题/间距选择器。
- 前两面的展开/折叠、第二面的 radio 切换及返回均生效；完成后恢复五面折叠。没有复现十几秒阻塞，工具调用往返不当作浏览器事件耗时。
- 桌面 2166×1279 下导入窗口关闭按钮能命中；此结果不是 iPhone 安全区的实机证明。
- 当前五面仅 181 节点；检查时未发现其子元素的运行中 CSS 动画，不能将发热直接归咎于这五面的动画。

没有向日志保存真实聊天正文、母本、Prompt、API Key 或响应 body。离线测试仅用原创材料；包内不包含用户私有内容。

## 仍待验证

1. 安装 1.5.20 后，iPhone Safari/TT/安卓及云酒馆的实际开关延迟、维修菜单、温升。
2. 实际服务商的生成耗时、截断/空流和错误率；没有消耗额外付费请求做验收。
3. 不同模型下的动态视觉、美学及跨五面的差异；提示冲突消除不等于模型每次都能兑现。
4. 酒馆启动、长聊天进入、正文流式结束后的发送恢复和家用网络；这些本次没有获得完整现场时间线，不声称全部正常。

安装前保留原扩展目录；覆盖完整包并整页刷新，确认界面版本 1.5.20。不要清空站点数据、母本或冷却记录。不满意可恢复备份目录；没有迁移删除原库。
